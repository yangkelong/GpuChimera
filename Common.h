#define PROGRAM_ONCE
#include <iostream>
#include <unordered_set>
#include <set>
#include <map>
#include <unordered_map>
#include <vector>
#include <random>
#include <algorithm>
#include <cfloat>
#include <time.h>
#include <fstream>
#include <iomanip>
#include <cstring>
#include <array>
#include <regex>
#include <cassert>
#include <algorithm>
// #include <string>

using uint32 = unsigned int;
using int64 = long long;
using float64 = double;
using myset = std::unordered_set<uint32>;
using orderDict = std::map<uint32, uint32>;
using mylist = std::vector<uint32>;
using mylistDouble = std::vector<double>;
using dict = std::unordered_map<uint32, uint32>;
using Coord = std::array<double, 3>;

// typedef double float64;
// typedef long long int64;
// typedef int int32;
// typedef unsigned int uint32;
// typedef std::unordered_set<uint32> myset;
// typedef std::unordered_map<uint32, uint32> dict;
// typedef std::map<uint32, uint32> orderDict;
// typedef std::vector<uint32> mylist;
// typedef std::vector<double> mylistDouble;

